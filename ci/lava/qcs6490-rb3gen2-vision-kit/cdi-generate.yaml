actions:
- deploy:
    rootfs_image: disk-ufs.img2
    qcomflash:
      headers:
        QLIAuthorization: Q_QLI_S3_TOKEN
      url: "{{BUILD_DOWNLOAD_URL}}/{{SUITE}}-flash-ufs.tar.gz"
      apply-overlay: true
    timeout:
      minutes: 20
    to: qdl
- boot:
    firehose_program: prog_firehose_ddr.elf
    rawprogram: rawprogram0.xml rawprogram1.xml rawprogram2.xml rawprogram3.xml rawprogram4.xml rawprogram5.xml
    patch: patch0.xml patch1.xml patch2.xml patch3.xml patch4.xml patch5.xml
    path: flash_qcs6490-rb3gen2-vision-kit_ufs
    storage: ufs
    debug: true
    timeout:
      minutes: 15
    method: qdl
- command:
    name: network_turn_on
- boot:
    auto_login:
      login_prompt: 'login:'
      username: debian
      password_prompt: 'Password'
      password: debian
      login_commands:
      - "debian"
      - "new password"
      - "new password"
      - sudo su
    method: minimal
    prompts:
    - root@debian
    - debian@debian
    - "Current password"
    - "New password"
    - "Retype new password"
    timeout:
      minutes: 3
- test:
    timeout:
      minutes: 10
    definitions:
    - from: git
      name: "cdi-generate"
      path: ci/lava-tests/cdi-generate/cdi-generate.yaml
      repository: https://github.com/{{GITHUB_REPOSITORY}}.git
      revision: '{{GITHUB_SHA}}'
context:
  test_character_delay: 10
device_type: qcs6490-rb3gen2-vision-kit
job_name: cdi-generator tests {{GITHUB_REPOSITORY}} {{GITHUB_RUN_ID}}-{{GITHUB_RUN_ATTEMPT}}
metadata:
  build-commit: '{{GITHUB_SHA}}'
priority: 50
tags:
- cambridge-lab
timeouts:
  job:
    minutes: 210
visibility: public
